[project]
name = "eos-sar"
authors = [
    { name = "Kayrros"},
    { name = "Centre Borelli"},
    { name = "individual collaborators" },
]
version = "0.43.1"
description = "Generic SAR (Synthetic Aperture Radar) processing algorithms"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
requires-python = ">=3.10,<3.14"
dependencies = [
    "numpy>=2.0.1",
    "python-dateutil>=2.9.0",
    "xmltodict>=0.14.0",
    "lxml>=5.3.1",
    "pyproj>=3.5.0",
    "opencv-python-headless>=4.11.0",
    "shapely>=2.0.4",
    "scipy (>=1.13.0, !=1.15.0.*,!=1.15.1,!=1.15.2)",  # Exclude certain versions, see https://github.com/Kayrros/eos-sar/issues/173
    "rasterio>=1.4.0",
    "ortools>=9.7.0",
    "requests>=2.32.3",
    "typing-extensions>=4.12.0",
    "h5py>=3.12.1",
    "fsspec[http,s3]>=2025.10.0",
]

[project.optional-dependencies]
teosar = [
    "tqdm>=4.67.0",
    "tifffile>=2024.1.0",
    "tensorflow-probability[tf]>=0.24.0",  # see https://github.com/tensorflow/probability/releases/tag/v0.24.0
    "siphash24>=1.0",  # pytools is required by pyopencl, and gives a warning: RecommendedHashNotFoundWarning: Unable to import recommended hash 'siphash24.siphash13',
    "pyopencl[pocl]>=2025.1",
]
teosar-light = [
    "tqdm>=4.67.0",
    "tifffile>=2024.1.0",
]

[tool.setuptools]
# 1. The Master Toggle: Tells setuptools to respect MANIFEST.in
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
# 2. The Manual Backup: Ensures .cl files are mapped to the package
"*" = ["*.cl"]

[build-system]
requires = ["setuptools", "cython", "numpy"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
# eos-sar requires >=3.10,<3.14 (see requires-python above)
build = "cp310-* cp311-* cp312-* cp313-*"
skip = ["*-musllinux*", "*_i686", "*-win32"]
build-frontend = "build[uv]"
test-command = "python -c \"import eos.sar.io; import eos.products.sentinel1.assembler\""

[dependency-groups]
docs = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.5.49",
    "mkdocstrings[python]>=0.27.0",
    "mkdocs-gen-files>=0.5.0",
    "mkdocs-literate-nav>=0.6.1",
    "mkdocs-section-index>=0.3.9",
    "ruff>=0.8.5",  # used by mkdocstrings to format signatures
]
dev = [
    # linting
    "ruff>=0.8.5",
    # mypy
    "mypy>=1.14.1",
    "types-python-dateutil>=2.9.0",
    "types-requests>=2.30.0",
    "types-setuptools>=69.2.0.20240317",
    # testing
    "pytest>=8.2.2",
    "pytest-xdist>=3.6.1",
    "pytest-rerunfailures>=16.0.1",
    # dem sources
    "srtm4[crop]>=1.2.5",
    "dem-stitcher>=2.5.12",
    # network access
    "boto3>=1.34.133",
    # usage/
    "fire>=0.6.0",
]

[tool.ruff]
line-length = 88
target-version = "py39"
extend-include = ["*.ipynb"]

[tool.ruff.lint]
select = [
    # defaults
    "F",
    "E",
    # isort
    "I001",
]
ignore = [
    "E741",  # Ambiguous variable name
    # line too long; ruff will still format what it can anyway
    # so this mostly to avoid triggers on comments
    "E501",
]

[tool.ruff.lint.per-file-ignores]
"*.ipynb" = ["E402"]  # it's OK if imports are not at the top of the file for notebooks

[tool.mypy]
install_types = true
ignore_missing_imports = true
check_untyped_defs = true
allow_redefinition = true
pretty = true
python_version = "3.10"

[tool.pytest.ini_options]
filterwarnings = [
    "ignore:Deprecated call to `pkg_resources\\.declare_namespace\\('.*'\\):DeprecationWarning",
    "ignore::DeprecationWarning:google.rpc",
]
